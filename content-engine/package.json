{
  "name": "ownet-content-engine",
  "version": "1.0.0",
  "private": true,
  "description": "OpticWise weekly content engine — reads Gmail, generates content via Claude, publishes via OWnet",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "tsx src/index.ts",
    "replay": "tsx scripts/replay-week.ts",
    "dry-run": "tsx scripts/dry-run.ts",
    "verify-secrets": "tsx scripts/verify-secrets.ts",
    "extract-template": "tsx scripts/extract-insights-template.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/ scripts/"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@slack/web-api": "^7.8.0",
    "dayjs": "^1.11.13",
    "googleapis": "^144.0.0",
    "marked": "^15.0.0",
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
